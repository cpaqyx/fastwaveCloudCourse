server:
  port: 8085
spring:
  application:
    name: service-gateway
  cloud:
    nacos:
      discovery:
#        server-addr: localhost:8841,localhost:8842,localhost:8843
        server-addr: localhost:8848,localhost:8842,localhost:8843
  main:
    allow-circular-references: true

feign:
  sentinel:
    enabled: true
